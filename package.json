{
  "name": "no-choice-lite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "deploy:pages": "npm run build && npx --yes wrangler pages deploy dist --project-name no-choice-lite"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
